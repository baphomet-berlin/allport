Description:
    Adds AllportContactable fields to your model and extends your model functionality 
    with the Allport::Concerns::Contactable concern.

Example:
    rails generate contact Thing

    This will create:
        /db/migrations/add_allport_contactable_to_thing
